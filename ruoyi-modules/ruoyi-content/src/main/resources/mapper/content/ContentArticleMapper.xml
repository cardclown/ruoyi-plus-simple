<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dromara.content.mapper.ContentArticleMapper">

    <select id="selectDeletedArticleIds" resultType="java.lang.Long">
        select article_id
        from content_article
        where del_flag = '1'
          and article_id in
        <foreach collection="articleIds" item="articleId" open="(" separator="," close=")">
            #{articleId}
        </foreach>
    </select>

    <update id="updateArticleStatus">
        update content_article
        set status = #{article.status},
            publish_by = #{article.publishBy,jdbcType=BIGINT},
            publish_time = #{article.publishTime,jdbcType=TIMESTAMP},
            update_by = #{article.updateBy,jdbcType=BIGINT},
            update_time = #{article.updateTime,jdbcType=TIMESTAMP}
        where article_id = #{article.articleId}
          and del_flag = '0'
    </update>

    <delete id="physicalDeleteByIds">
        delete from content_article
        where del_flag = '1'
          and article_id in
        <foreach collection="articleIds" item="articleId" open="(" separator="," close=")">
            #{articleId}
        </foreach>
    </delete>
</mapper>
